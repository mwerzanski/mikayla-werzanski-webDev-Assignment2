import React from 'react';
import { useState } from 'react';

/* export default () => {
    const [gridHeight, updateGridHeight] = useState(10);
    const [gridWidth, updateGridWidth] = useState(10);
    return <button onClick={() => updateGridHeight}>Create Simulation</button>;
}
 */
